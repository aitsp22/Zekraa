<div class="container my-5">
  <section class="s-block s-block--categories">
    <salla-slider type="carousel" auto-play loop="true" show-controls="false">
    <div slot="items">
  
    {% for slide in component.salesslider %}
    <a href="{{link('offers')}}">
      <img class="rounded m-1 w-full" src="{{slide.sliderimg}}" />
    </a>
  
      {% endfor %}
  
    </div>
  
    </salla-slider>

  <br/>
  <div class="grid grid-cols-2  lg:gap-4 gap-2">
    <a href="{{link('offers')}}">
  <img class="lg:rounded lg:m-1 m-0" src="{{component.saleimgright}}" />
  </a>
  
  <a href="{{link('offers')}}">
  <img class="lg:rounded lg:m-1 m-0" src="{{component.saleimgleft}}" />
  </a>
  </div>
  
  </div>
  
  </section>
</div>
